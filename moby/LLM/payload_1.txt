"Read the first 0x48 bytes as the MobyClassHeader: at 0x00 read a little‐endian s32 submesh_table_offset; at 0x04 read four u8s for high_lod_count, low_lod_count, metal_count, metal_begin; at 0x08 read u8 joint_count; at 0x0C read u8 sequence_count; at 0x10 read s32 collision_ptr (unused); at 0x14 read s32 skeleton_ptr; at 0x18 read s32 common_trans_ptr; at 0x1C read s32 joints_ptr (unused); at 0x20 read s32 gif_usage_ptr; at 0x24 read f32 scale; skip sound_defs, ban­gles, mip_dist, corncob, bounding_sphere, glow_rgba, mode_bits, type, mode_bits2. Seek to offset 0x48 and for sequence_count entries read 4-byte s32 sequence_ptrs; at each sequence_ptr parse 28-byte MobySequenceHeader (4×f32 bounding sphere, 4×u8 flags, 2×u32 where the first is animation_info_ptr); at animation_info_ptr parse 16 bytes DeadlockedMobySequenceDataHeader (4×u8 including spr_dma_qwc, 3×u32 where second is dma_data_offset) then read spr_dma_qwc×16 bytes VIF packet list, unpacking each 32-bit word into cmd=(bits24-30), count=(bits16-23 or 256), and if cmd in 0x60–0x7F treat as UNPACK: extract vnvl=(bits24-27), vn=(vnvl>>2)&3, vl=vnvl&3, addr=bits0-21, elem_size=((32>>vl)*(vn+1))//8. Seek to gif_usage_ptr, skip 8-byte GIF tag, read next word cmd for DIRECT/DIRECTHL, count qwc, read qwc×16 bytes then interpret as u16 index list. Seek to skeleton_ptr and read joint_count Mat4s (16×f32 each) into np.ndarray. Seek to common_trans_ptr and for each of joint_count entries read 3×f32 translation, u16 parent_off, u16 flags and compute translation×scale, parent_index=parent_off==0?–1:parent_off/0x40. Seek to submesh_table_offset and for each of (metal_begin+metal_count) entries read 16-byte SubmeshEntry: u32 vif_list_offset, u16 vif_list_size, u16 unused, u32 vertex_offset, u8 vertex_data_size, u8 unknown_d, u8 unknown_e, u8 transfer_vertex_count. For each submesh seek to vif_list_offset, read vif_list_size×16 bytes and unpack as above; sum each UNPACK count×elem_size in order and verify that running_vif_offset + total_bytes == vertex_offset; then seek to vertex_offset and for transfer_vertex_count entries read 16 bytes per MobyVertex: u16 low_halfword (vertex_index=low_halfword&0x1FF, joint_index=low_halfword>>9), u8 vu0_src1, u8 vu0_src2, u8 weight1, u8 weight2, u8 vu0_dst1, u8 vu0_dst2, u8 normal_az, u8 normal_el, s16 x, s16 y, s16 z and convert normals from az/el to unit vector and positions from s16×scale to world units; optionally look for UNPACK streams with elem_size==8 at known UV addr to extract (u,v) pairs; invert bone world matrices for inverseBindMatrices. Collision, joints_ptr, sound_defs, material flags and bounding_sphere were read but not parsed, and texture data lives outside in separate .png." Try to read "moby.bin"
